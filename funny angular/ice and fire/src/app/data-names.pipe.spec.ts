import { DataNamesPipe } from './data-names.pipe';

describe('DataNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new DataNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
